VULKAN API практика
=
Проект 1. Задача 12
--
***Что осилили?***
--
1. Настроили флаги отображения уровней сообщений в DEBUG
1. Настроили дублирование журнала DEBUG в файл
1. Настроили передача при копировании буферов с применением двух командных буферов и двух заборов (vk::Fence)
1. Для Application и VulkanSystem создали структуры переменных состояния. Теперь всё аккуратно сложено в одном месте. Это - те самые структуры, которые (или часть которых) я хочу иметь возможность сохранять в файл
1. Создали макет набора дескрипторов (vk::UniqueDescriptorSetLayout) и привязали его к макету конвейера
1. Создали пул дескрипторов (vk::UniqueDescriptorPool)
1. СОздали наборы дескрипторов (vk::UniqueDescriptorSet)
1. Привязали Uniform buffers к дескрипторам
1. Настроили матричные преобразования Uniform buffers для отображения курсора в позицию мышки и в правильном масштабе
1. Создали объекты перекрестия и сетки (как часть сцены) + вершинные буферы отдельно для перекрестия и сцены + индекные буферы перекрестия и сцены + трансформации перекрестия и сцены
1. Создали функции setCrosshair_, setGrid_
1. Вместо разных названий для буферов сделаны массивы с применением Enum Class для доступа к перекрестию или сцене (как это сделано для шейдерных модулей). Было: crosshairVertexIndices_, стало: vertexIndices_[static_cast<size_t>(ObjectFlag::eCrosshair)]. Теперь можно добавлять сколько угодно блоков примитивов и связанных с ними объектов VULKAN

***Что дальше?***
--
1. Настроить приближение, перемещение и вращение 3D координат модели колёсиком
1. ООтображать сетку и перекрестие в зависимости от текущей координатной системы модели. Матричные преобразования (параллельные и перспективные проекции)
1. Отображать координаты курсора (координаты не мыши в окне WinAPI, а координаты модели)

1. Настроить использование двух или нескольких std::thread
1. При обновлении координат курсора сразу обновление трансформаций? Как это сделать? В отдельном потоке с применением условной переменной?
1. Заменить флаги ( 1) переместился курсор + 2) время нового рендера) атомарными флагами? + Внедрить использование условных переменных

1. Настроить контекстное меню и окно параметров
1. Настроить сохранение параметров в бинарный файл (для начала цвет фона + размер и цвет пересечения)
1. Настроить возможность сохранения модели и параметров

***В чём пока не могу разобраться***
--
1. Как последовательно выполнить несколько конвейеров. К примеру, сначала отрисовка граней, затем отрисовка линий, затем отрисовка точек. Динамическое состояние не позволяет переключаться на лету. Или я пока неправильно понимаю ситуацию